[buildout]
extensions =
    mr.developer
parts = {{project}}
versions = versions
show-picked-versions = true
develop = .

[{{project}}]
recipe = zc.recipe.egg
interpreter = python
eggs =
    {{project}}

[versions]
